# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Reference video + local scratch. The video IS the spec for the mechanic (see CLAUDE.md)
# but it is a 23MB phone capture, so it stays out of the repo.
/Manythings/

# Headless screenshot runs (scripts/shot.mjs) — captures plus a whole Chrome profile, which
# is thousands of files and will happily walk into a commit if this line is missing.
scripts/.shots/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.sw?

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Environment / secrets
.env
.env.*
!.env.example
